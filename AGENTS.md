# Repository Instructions

Before creating, reading for modification, editing, moving, or deleting any project file, every AI coding agent **must read `/docs/AGENTS.md` in full**.

The rules in `/docs/AGENTS.md` apply to the entire repository, not only to the `docs` directory. They are mandatory unless the user's current explicit instruction overrides them.

All Markdown project documentation must be stored under `/docs`. This root `AGENTS.md` is the only exception because it is the repository-level AI instruction entry point.
